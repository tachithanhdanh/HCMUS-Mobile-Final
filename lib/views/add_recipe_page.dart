import 'package:flutter/material.dart';

class AddRecipePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Add Recipe')),
      body: Center(
        child: Text('This is Add Recipe Page'),
      ),
    );
  }
}
