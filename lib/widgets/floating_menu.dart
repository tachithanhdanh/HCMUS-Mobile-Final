// views/widgets/floating_menu.dart
import 'package:flutter/material.dart';

class FloatingMenu extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      onPressed: () {
        // Mở menu tùy chọn
      },
      child: Icon(Icons.add),
    );
  }
}
